<div id="brand_list">
	<ul>
	<%- brand_list.brands.each do |brand| -%>
		<li <%= "class='current'" if brand == @brand %>><%= link_to(brand.name, [brand_list, brand]) %></li>
	<%- end -%>
	</ul>
</div>